<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:behaviors="clr-namespace:Countries.Behaviors;assembly=Countries"
             xmlns:local="clr-namespace:Countries"
             x:Class="Countries.MainPage">
    <ListView ItemsSource="{Binding Countries}"
                  VerticalOptions="FillAndExpand" HasUnevenRows="true"
                  RefreshCommand="{Binding RefreshCommand}" IsPullToRefreshEnabled="true"
                  IsRefreshing="{Binding IsBusy, Mode=OneWay}"
                  CachingStrategy="RecycleElement">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout>
                        <Label Text="{Binding name}" LineBreakMode="NoWrap" FontSize="16"/>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

</ContentPage>
